
export const Upto2Digit  = (val:number) :number =>{
  return Math.round(val*100)/100;
}

export const convert  = (val:number) :number =>{
  return Math.round(val*100)/100;
}

export const string2Upper = (str: string) : string => {
  return str.toUpperCase();
}